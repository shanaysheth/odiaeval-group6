# Pinned to the versions the reported results were produced with.
# Colab (Sept 2026) ships these; the nano server may need them installed.
torch==2.11.0
transformers==5.16.1
datasets==4.0.0
pyarrow==18.1.0
huggingface_hub>=0.35
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
