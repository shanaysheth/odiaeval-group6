# Data and checkpoints are NOT committed — too large, and regenerable.
work/
raw/
interim/
processed/
checkpoints/
native/
*.parquet
*.jsonl
*.safetensors
*.bin
*.ckpt

# Results artefacts ARE committed: they are what makes the reported numbers
# verifiable. JSON only — nothing large.
!artifacts/
!artifacts/*.json
!logs/
!logs/*.txt

.env
*.token
hf_token*
.hf_token

__pycache__/
*.py[cod]
.ipynb_checkpoints/
.DS_Store
venv/
.venv/
